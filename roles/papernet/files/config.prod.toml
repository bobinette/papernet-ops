# Dev configuration

# ----------------------------------------
# Auth service
[auth]
key = "configuration/hs256.json"

[auth.cayley]
store = "data/auth.graph"
# Auth service
# ----------------------------------------

# ----------------------------------------
# Oauth service
[oauth]
bolt="data/oauth.db"

[oauth.auth]
enabled=false

[oauth.google]
enabled=true
file="configuration/oauth_google.json"
# Oauth service
# ----------------------------------------

# ----------------------------------------
# Imports service
[imports]
paperURL="http://127.0.0.1:1705/papers" # Mono server => it works
key = "configuration/hs256.json"

[imports.bolt]
store="data/imports.db"
# Imports service
# ----------------------------------------

# ----------------------------------------
# legacy
[bolt]
store = "data/papernet.db"

[bleve]
store = "data/papernet.index"
